---
const { title, description, image, link } = Astro.props;
---

<article class="card">

<img src={image} alt={title}>

<h3>{title}</h3>

<p>{description}</p>

<a href={link}>View Project →</a>

</article>